# AppArmor profile for Virtual Doorlock System (VDS) App

profile vds {
  network inet  stream,
  network inet  dgram,
  network inet6 stream,
  network inet6 dgram,

  /app/vds/**  r,
  /run.sh      rx,
  /data/**     rw,
  /tmp/**      rw,
}
